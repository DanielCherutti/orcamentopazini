PENDENTE: (nenhum no momento)

---
CONCLUÍDOS:

X Seleção de grupos: redesenho parecido com produtos — busca por nome, segmentação por grupo, quantidades e seleção de itens; adicionar de vários grupos em um passo; "Selecionar todos" / "Desmarcar todos" por grupo.
  - [x] Isolamento de seleção: selecionar em um grupo desabilita no outro, evitando duplicatas e conflitos de IDs SurrealDB v3 (ckey = idx::productId).

X Mover produtos entre grupos e para fora de grupo — updateItemGroupInBlockAction + Select "Mover para grupo" no ItemRow do compositor.

X Na edição de fotos trazer os produtos do orçamento na listagem lateral — availableItems passado no compositor (SectionRenderer), no escopo (SectionDetail e LocationDetail) e já existia no editor (SectionSceneCard, LocationDetailPanel).

X Na adição de fotos trazer do orçamento e dos grupos; botão de catálogo na barra do editor — availableItems cobre orçamento; grupos na aba Catálogo do dock; botão Catálogo na Toolbar do anotador (toggle do dock).

X Tirar icones de pasta e folha dos nomes dos titulos, deixar sem icones mesmo, somente texto com as numeraçoes coerentes.

X O parte de ocultar a barra lateral deve funcionar para o escopo tambem — toggle "Ocultar índice" adicionado igual ao compositor.

X A regra de nomenclatura para sessoes tanto do orcamento quanto da aba escopo e uppercase e de subsessoes como CamelCase — aplicado automaticamente ao salvar.

X Busca de produtos nao esta case sensitive — corrigido com string::lowercase() no SurrealDB.

X Ao buscar e limpar parece que nao ta resetando — corrigido, reload imediato ao limpar.

X No escopo, ao clicar no indice da sessao deveria trazer os trechos ja abertos na visualizaçao e se clicar nos trechos traz só ele aberto — trechos agora são botões clicáveis que navegam para o SectionDetail.

X deixar a tela de edicao de imagems 100% de tamanho — dialog fullscreen (w-screen h-screen); flex-1 no inner container; overflow-hidden no wrapper; footer duplicado removido.

X Em todos os uploads de imagem permitir drag and drop e ctrl+v — suporte completo adicionado no dialog de anotação.

X deu alguns bugs visuais no arrastar e soltar — DndContext interno do grupo agora usa id único para isolar eventos do contexto externo.

X Implementar soft delete em tudo, some da tela mas fica no banco — deleted_at aplicado em blocks, itens, locais, seções e imagens. Queries de leitura filtram deleted_at IS NONE.

X no orçamento temos um item fixo chamado Escopo, ele pode ser movido, mas nao encorporado a outras sessoes como subsessao e nao pode ser deletado — drag desabilitado, botão delete oculto, drop-into bloqueado para scope.

X Bugou as numeracoes, precisa controlar tanto em orcamento, por exemplo, escopo ficou na posiçáo 5, dentro da aba escopo deve refletir o 5 e os subsequentes dentro dele. O mesmo vale para os trechos — numeração hierárquica N.X (locais) e N.X.Y (trechos) baseada no número do bloco escopo no compositor.

X na edicao da imagem alertar caso tenha alteracao nao salva antes de sair e perguntar se quer salvar ou descartar — AlertDialog de confirmação implementado. Autosave zera o flag de "pendente".

X bug visual ao editar imagem, ta dando um pulinho na imagem quando vai salvar automaticamente — reset/export/restore no mesmo requestAnimationFrame.

X DnD cross-parent no compositor: arrastar sessões para dentro de outras sessões — DndContext único no topo, DroppableSessionInto para zonas de drop-into, moveBlockToParentAction no servidor.

X Remover botão "mover para" (MoveRight) do escopo — substituído pelo DnD.

X Reordenar elementos do painel de Local: Título → Fotos → Descrição → Trechos.

X Reordenar elementos do painel de Trecho: Título → Fotos → Produtos → Descrição.
